import { BrowserRouter } from "react-router-dom";
console.log(process.env);
const App = () => {
  return (
    <div>
      <h1>Dime</h1>
      <BrowserRouter></BrowserRouter>
    </div>
  );
};

export default App;
